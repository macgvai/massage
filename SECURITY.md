# 🔐 Безопасность админки

## Текущая защита

### 1. Защита паролем на уровне компонента
- **AuthWrapper** проверяет доступ ко всем страницам админки
- **Cookie-авторизация** с временем жизни 24 часа
- **Встроенная форма входа** в layout админки
- **API авторизации** `/api/admin/auth`

### 2. Настройка пароля

#### В файле .env.local.js
```bash
# Добавьте в конец файла:
ADMIN_PASSWORD=ваш_сложный_пароль_здесь
```

### 3. Текущий пароль
По умолчанию: `massage_admin_2024`

**⚠️ ВАЖНО: Обязательно смените пароль перед публикацией сайта!**

## Как использовать

### 1. Перейти в админку
```
https://ваш-сайт.ru/admin
```

### 2. Ввести пароль
- По умолчанию: `massage_admin_2024`
- Или ваш пароль из `.env.local.js`

### 3. Работать в админке
- Сессия действует 24 часа
- Кнопка "🚪 Выйти" в правом верхнем углу

## Что защищено

✅ **Все страницы админки**:
- `/admin` - главная панель
- `/admin/help` - справка
- `/admin/test-*` - тестовые страницы

✅ **Все API админки**:
- `/api/admin/config` - сохранение настроек
- `/api/admin/upload` - загрузка изображений
- `/api/admin/current-images` - получение изображений

## Преимущества текущего решения

✅ **Простота** - нет сложного middleware
✅ **Надежность** - работает на уровне React компонентов
✅ **Удобство** - встроенная форма входа
✅ **Безопасность** - cookie-сессии с истечением
✅ **UX** - плавные переходы без перезагрузок

## Смена пароля

1. **Откройте файл** `.env.local.js`
2. **Добавьте или измените**:
   ```bash
   ADMIN_PASSWORD=ваш_новый_пароль
   ```
3. **Перезапустите сервер**

## Рекомендации

1. **Используйте сложный пароль**: `MassageSalon2024!@#`
2. **Включите HTTPS** в продакшене
3. **Не делитесь паролем** с посторонними
4. **Выходите из админки** после работы
5. **Регулярно меняйте пароль**

## Восстановление доступа

Если забыли пароль:
1. Измените `ADMIN_PASSWORD` в `.env.local.js`
2. Перезапустите сервер
3. Очистите cookies браузера для сайта